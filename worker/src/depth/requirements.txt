# Dependencias de Python para Depth Anything (Fase 8.1, corre en la PC).
# Instalación sugerida en un venv:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# Para GPU, instalá el torch que corresponda a tu CUDA desde pytorch.org.
torch
transformers>=4.45
pillow
numpy
