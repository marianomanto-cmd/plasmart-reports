# === SUPABASE ===
# Public (frontend) - se exponen al cliente
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Secret (server-side only) - NUNCA exponer al cliente
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# === ANTHROPIC ===
# Secret (server-side only) - usado por la Edge Function de análisis
ANTHROPIC_API_KEY=sk-ant-api03-...

# === GOOGLE OAUTH ===
# Configurado en Supabase Auth, no se usa directamente en el código
# Pero documentamos los valores aquí para referencia:
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-xxx
# GOOGLE_HOSTED_DOMAIN=transfil.com.ar

# === GOOGLE SERVICE ACCOUNT (para Drive/Sheets) ===
# Se guarda como secret en Supabase Edge Functions, no acá
# Documentado solo para referencia:
# GOOGLE_SERVICE_ACCOUNT_EMAIL=xxx@xxx.iam.gserviceaccount.com
# GOOGLE_SERVICE_ACCOUNT_KEY=---BEGIN PRIVATE KEY---...

# === APP ===
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === RESEND (reporte semanal por mail) ===
# Secret (server-side only). Generar en dashboard.resend.com.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxx
# Remitente. El dominio debe estar verificado en Resend con SPF + DKIM.
REPORT_FROM_EMAIL=reportes@transfil.com.ar
# Destinatarios separados por coma. Hardcoded en env, NO viene de la DB.
REPORT_RECIPIENTS=mariano@transfil.com.ar,otro@transfil.com.ar

# === CRON (auth shared secret entre pg_cron y /api/cron/weekly-report) ===
# String random largo (32+ chars). Mismo valor en Vercel y en pg_cron.
CRON_SECRET=long_random_string_replace_me
