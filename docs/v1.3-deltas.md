# Plasmart Reports — v1.3 · Profundización Google Ads

**Adendum al plan de desarrollo. App ya en producción.**

**Branch sugerida:** `feature/v1.3-gads-deep` desde `main`.
**Última migración aplicada:** `20260512140000_v12_ai_cooldown_rpc.sql`.

Este doc lista los cambios diferenciales que se suman a lo que ya existe.
Cada PR identifica qué archivos del repo se tocan (modificados) y cuáles
son nuevos.

---

## Objetivo

Sumar al análisis de Google Ads tres niveles de profundidad que hoy no
existen:

- **Ad Groups + Asset Groups** (unificados con `group_type`).
- **Keywords + Search Terms** (solo para search/demand gen).
- **Geo a nivel provincia** (User Location Report).

UI: drill-down inline en la pestaña `Detalle` (sin pestaña nueva).
IA: bloque condicional en el prompt del Resumen actual (sin route nueva).

Meta, GA4 y la pestaña Comparativa no se tocan.

---

## Decisiones ya tomadas (no re-discutir)

1. Tabla unificada `dim_ad_group` con `group_type` (`ad_group` | `asset_group`).
2. User Location Report (de dónde están los usuarios), no Geographic.
3. Drill-down inline, no pestaña nueva.
4. Mejora al prompt del Resumen, no analizador nuevo.
5. Argentina-only para geo: `country_code = 'AR'` implícito, `region_code` ISO 3166-2.
6. PMax con asset groups visibles, sin keywords/search terms (limitación de la plataforma).

---

## Orden de PRs

```
PR 1 (migración) ────────┐
                         ├─→ PR 3 (ingesta) ─→ PR 4 (UI) ─→ PR 5 (IA)
PR 2 (GAds + AppScript) ─┘
```

PR 1 y PR 2 son paralelos. PR 4 requiere 2-3 días de ingesta exitosa en
prod antes de empezar.

---

## PR 1 — Migración de schema

### Archivos nuevos

`supabase/migrations/20260513120000_v13_gads_deep.sql`

### Archivos modificados

Ninguno. La función `refresh_all_materialized_views` se actualiza dentro
de la misma migración con `create or replace`.

### Contenido completo del archivo

```sql
-- ============================================================
-- v1.3 · Profundización Google Ads
-- Ad groups + asset groups unificados, keywords, search terms,
-- geo por provincia.
-- ============================================================

-- ============================================================
-- TABLAS
-- ============================================================

create table public.dim_ad_group (
  id uuid primary key default gen_random_uuid(),
  campaign_id uuid not null references public.dim_campaign(id) on delete cascade,
  external_id text not null,
  name text not null,
  group_type text not null check (group_type in ('ad_group', 'asset_group')),
  status text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (campaign_id, external_id)
);

create index idx_dim_ad_group_campaign on public.dim_ad_group(campaign_id);

create table public.fact_ad_group_daily (
  date date not null,
  ad_group_id uuid not null references public.dim_ad_group(id) on delete cascade,
  impressions bigint not null default 0,
  clicks bigint not null default 0,
  conversions numeric(10,2) not null default 0,
  cost_ars numeric(18,2) not null default 0,
  revenue_ars numeric(18,2) not null default 0,
  ctr numeric generated always as (
    case when impressions > 0 then clicks::numeric / impressions else 0 end
  ) stored,
  cpc numeric generated always as (
    case when clicks > 0 then cost_ars / clicks else 0 end
  ) stored,
  cpa numeric generated always as (
    case when conversions > 0 then cost_ars / conversions else 0 end
  ) stored,
  raw_payload jsonb,
  primary key (date, ad_group_id)
);

create index idx_fact_ad_group_date on public.fact_ad_group_daily(date);

create table public.dim_keyword (
  id uuid primary key default gen_random_uuid(),
  ad_group_id uuid not null references public.dim_ad_group(id) on delete cascade,
  external_id text not null,
  keyword_text text not null,
  match_type text not null check (match_type in ('EXACT', 'PHRASE', 'BROAD')),
  status text,
  quality_score smallint check (quality_score between 1 and 10),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (ad_group_id, external_id)
);

create index idx_dim_keyword_ad_group on public.dim_keyword(ad_group_id);

create table public.fact_keyword_daily (
  date date not null,
  keyword_id uuid not null references public.dim_keyword(id) on delete cascade,
  impressions bigint not null default 0,
  clicks bigint not null default 0,
  conversions numeric(10,2) not null default 0,
  cost_ars numeric(18,2) not null default 0,
  avg_position numeric(4,2),
  ctr numeric generated always as (
    case when impressions > 0 then clicks::numeric / impressions else 0 end
  ) stored,
  cpc numeric generated always as (
    case when clicks > 0 then cost_ars / clicks else 0 end
  ) stored,
  cpa numeric generated always as (
    case when conversions > 0 then cost_ars / conversions else 0 end
  ) stored,
  raw_payload jsonb,
  primary key (date, keyword_id)
);

create index idx_fact_keyword_date on public.fact_keyword_daily(date);

create table public.fact_search_term_daily (
  date date not null,
  ad_group_id uuid not null references public.dim_ad_group(id) on delete cascade,
  search_term text not null,
  matched_keyword_id uuid references public.dim_keyword(id) on delete set null,
  match_type text,
  impressions bigint not null default 0,
  clicks bigint not null default 0,
  conversions numeric(10,2) not null default 0,
  cost_ars numeric(18,2) not null default 0,
  ctr numeric generated always as (
    case when impressions > 0 then clicks::numeric / impressions else 0 end
  ) stored,
  cpc numeric generated always as (
    case when clicks > 0 then cost_ars / clicks else 0 end
  ) stored,
  cpa numeric generated always as (
    case when conversions > 0 then cost_ars / conversions else 0 end
  ) stored,
  raw_payload jsonb,
  primary key (date, ad_group_id, search_term)
);

create index idx_fact_search_term_date on public.fact_search_term_daily(date);
create index idx_fact_search_term_waste
  on public.fact_search_term_daily(cost_ars desc)
  where conversions = 0 and cost_ars > 0;

create table public.fact_campaign_geo_daily (
  date date not null,
  campaign_id uuid not null references public.dim_campaign(id) on delete cascade,
  region_code text not null default 'unknown',  -- AR-X (ISO 3166-2)
  region_name text,
  impressions bigint not null default 0,
  clicks bigint not null default 0,
  conversions numeric(10,2) not null default 0,
  cost_ars numeric(18,2) not null default 0,
  ctr numeric generated always as (
    case when impressions > 0 then clicks::numeric / impressions else 0 end
  ) stored,
  cpc numeric generated always as (
    case when clicks > 0 then cost_ars / clicks else 0 end
  ) stored,
  cpa numeric generated always as (
    case when conversions > 0 then cost_ars / conversions else 0 end
  ) stored,
  raw_payload jsonb,
  primary key (date, campaign_id, region_code)
);

create index idx_fact_geo_date on public.fact_campaign_geo_daily(date);

-- ============================================================
-- RLS (mismo patrón que las tablas existentes)
-- Copiar la política exacta desde 20260507220902_initial_schema.sql
-- ============================================================

alter table public.dim_ad_group enable row level security;
alter table public.fact_ad_group_daily enable row level security;
alter table public.dim_keyword enable row level security;
alter table public.fact_keyword_daily enable row level security;
alter table public.fact_search_term_daily enable row level security;
alter table public.fact_campaign_geo_daily enable row level security;

-- IMPORTANTE: replicar la policy de lectura de @transfil.com.ar
-- que ya existe en las tablas dim_campaign / fact_campaign_daily.
-- (Claude Code: leé una migración previa para copiar el patrón exacto.)

-- ============================================================
-- VISTAS MATERIALIZADAS
-- ============================================================

create materialized view public.mv_ad_group_summary as
select
  ag.id as ad_group_id,
  ag.campaign_id,
  ag.name as ad_group_name,
  ag.group_type,
  c.name as campaign_name,
  c.publisher,
  f.date,
  f.impressions,
  f.clicks,
  f.conversions,
  f.cost_ars,
  f.ctr,
  f.cpc,
  f.cpa
from public.fact_ad_group_daily f
join public.dim_ad_group ag on ag.id = f.ad_group_id
join public.dim_campaign c on c.id = ag.campaign_id;

create unique index idx_mv_ad_group_summary_pk
  on public.mv_ad_group_summary(ad_group_id, date);
create index idx_mv_ad_group_summary_date on public.mv_ad_group_summary(date);
create index idx_mv_ad_group_summary_campaign on public.mv_ad_group_summary(campaign_id);

create materialized view public.mv_search_term_waste as
select
  st.ad_group_id,
  ag.campaign_id,
  c.name as campaign_name,
  ag.name as ad_group_name,
  st.search_term,
  st.match_type,
  sum(st.cost_ars) as total_cost,
  sum(st.impressions) as total_impressions,
  sum(st.clicks) as total_clicks,
  min(st.date) as first_seen,
  max(st.date) as last_seen
from public.fact_search_term_daily st
join public.dim_ad_group ag on ag.id = st.ad_group_id
join public.dim_campaign c on c.id = ag.campaign_id
where st.cost_ars > 0
group by st.ad_group_id, ag.campaign_id, c.name, ag.name,
         st.search_term, st.match_type
having sum(st.conversions) = 0;

create unique index idx_mv_search_term_waste_pk
  on public.mv_search_term_waste(ad_group_id, search_term, match_type);
create index idx_mv_search_term_waste_campaign on public.mv_search_term_waste(campaign_id);

create materialized view public.mv_geo_summary as
select
  g.campaign_id,
  c.name as campaign_name,
  g.region_code,
  g.region_name,
  g.date,
  g.impressions,
  g.clicks,
  g.conversions,
  g.cost_ars,
  g.ctr,
  g.cpc,
  g.cpa
from public.fact_campaign_geo_daily g
join public.dim_campaign c on c.id = g.campaign_id;

create unique index idx_mv_geo_summary_pk
  on public.mv_geo_summary(campaign_id, region_code, date);
create index idx_mv_geo_summary_date on public.mv_geo_summary(date);
create index idx_mv_geo_summary_campaign on public.mv_geo_summary(campaign_id);

-- ============================================================
-- ACTUALIZAR refresh_all_materialized_views
-- (existe desde 20260508120000; extender con las 3 nuevas)
-- ============================================================

create or replace function public.refresh_all_materialized_views()
returns void
language plpgsql
security definer
as $$
begin
  -- Existentes (copiar el cuerpo actual antes de modificar)
  refresh materialized view concurrently public.mv_campaign_weekly;
  refresh materialized view concurrently public.mv_campaign_monthly;
  refresh materialized view concurrently public.mv_publisher_summary;

  -- Nuevas v1.3
  refresh materialized view concurrently public.mv_ad_group_summary;
  refresh materialized view concurrently public.mv_search_term_waste;
  refresh materialized view concurrently public.mv_geo_summary;
end;
$$;

-- ============================================================
-- RPCs para PR 5 (IA)
-- Patrón: igual que dashboard_anomalies, sql stable, grant authenticated
-- ============================================================

-- Nota: total_clicks numeric (no bigint) porque sum(bigint) devuelve
-- numeric en Postgres. Declarar bigint hace fallar el CREATE FUNCTION
-- con check_function_bodies = on.
create or replace function public.top_wasted_search_terms(
  p_start date,
  p_end date,
  p_limit int default 5
)
returns table (
  campaign_name text,
  ad_group_name text,
  search_term text,
  match_type text,
  total_cost numeric,
  total_clicks numeric
)
language sql stable as $$
  select
    c.name,
    ag.name,
    st.search_term,
    st.match_type,
    sum(st.cost_ars)::numeric,
    sum(st.clicks)::numeric
  from public.fact_search_term_daily st
  join public.dim_ad_group ag on ag.id = st.ad_group_id
  join public.dim_campaign c on c.id = ag.campaign_id
  where st.date between p_start and p_end
    and st.cost_ars > 0
  group by c.name, ag.name, st.search_term, st.match_type
  having sum(st.conversions) = 0
  order by sum(st.cost_ars) desc
  limit p_limit;
$$;

create or replace function public.underperforming_ad_groups(
  p_start date,
  p_end date
)
returns table (
  campaign_name text,
  ad_group_name text,
  ad_group_ctr numeric,
  campaign_avg_ctr numeric,
  cost_ars numeric
)
language sql stable as $$
  with ag_metrics as (
    select
      ag.campaign_id,
      ag.id as ad_group_id,
      ag.name as ad_group_name,
      sum(f.impressions) as imp,
      sum(f.clicks) as clk,
      sum(f.cost_ars) as cost,
      case when sum(f.impressions) > 0
           then sum(f.clicks)::numeric / sum(f.impressions)
           else 0 end as ctr
    from public.fact_ad_group_daily f
    join public.dim_ad_group ag on ag.id = f.ad_group_id
    where f.date between p_start and p_end
    group by ag.campaign_id, ag.id, ag.name
  ),
  campaign_avg as (
    select
      campaign_id,
      avg(ctr) filter (where imp > 100) as avg_ctr
    from ag_metrics
    group by campaign_id
  )
  select
    c.name,
    m.ad_group_name,
    m.ctr,
    ca.avg_ctr,
    m.cost
  from ag_metrics m
  join campaign_avg ca on ca.campaign_id = m.campaign_id
  join public.dim_campaign c on c.id = m.campaign_id
  where m.ctr < ca.avg_ctr * 0.5
    and m.imp > 100
    and m.cost > 0
  order by m.cost desc;
$$;

create or replace function public.low_quality_keywords(
  p_start date,
  p_end date,
  p_min_cost numeric default 500
)
returns table (
  campaign_name text,
  ad_group_name text,
  keyword_text text,
  match_type text,
  quality_score smallint,
  total_cost numeric
)
language sql stable as $$
  select
    c.name,
    ag.name,
    k.keyword_text,
    k.match_type,
    k.quality_score,
    sum(f.cost_ars)::numeric
  from public.fact_keyword_daily f
  join public.dim_keyword k on k.id = f.keyword_id
  join public.dim_ad_group ag on ag.id = k.ad_group_id
  join public.dim_campaign c on c.id = ag.campaign_id
  where f.date between p_start and p_end
    and k.quality_score is not null
    and k.quality_score < 5
  group by c.name, ag.name, k.keyword_text, k.match_type, k.quality_score
  having sum(f.cost_ars) >= p_min_cost
  order by sum(f.cost_ars) desc;
$$;

create or replace function public.geo_outliers(
  p_start date,
  p_end date
)
returns table (
  campaign_name text,
  region_name text,
  region_cpa numeric,
  national_avg_cpa numeric,
  cost_ars numeric,
  conversions numeric
)
language sql stable as $$
  with by_region as (
    select
      g.campaign_id,
      g.region_code,
      g.region_name,
      sum(g.cost_ars) as cost,
      sum(g.conversions) as conv,
      case when sum(g.conversions) > 0
           then sum(g.cost_ars) / sum(g.conversions)
           else null end as cpa
    from public.fact_campaign_geo_daily g
    where g.date between p_start and p_end
    group by g.campaign_id, g.region_code, g.region_name
  ),
  national as (
    select
      campaign_id,
      sum(cost) / nullif(sum(conv), 0) as avg_cpa
    from by_region
    group by campaign_id
  )
  select
    c.name,
    r.region_name,
    r.cpa,
    n.avg_cpa,
    r.cost,
    r.conv
  from by_region r
  join national n on n.campaign_id = r.campaign_id
  join public.dim_campaign c on c.id = r.campaign_id
  where r.cpa is not null
    and n.avg_cpa is not null
    and r.cpa > n.avg_cpa * 2
    and r.cost > 0
  order by r.cost desc;
$$;

grant execute on function public.top_wasted_search_terms to authenticated;
grant execute on function public.underperforming_ad_groups to authenticated;
grant execute on function public.low_quality_keywords to authenticated;
grant execute on function public.geo_outliers to authenticated;
```

### Validación

```sql
-- En SQL editor de Supabase, post-aplicación:
select table_name from information_schema.tables
where table_schema = 'public'
  and table_name in (
    'dim_ad_group','fact_ad_group_daily','dim_keyword',
    'fact_keyword_daily','fact_search_term_daily','fact_campaign_geo_daily'
  );  -- esperado: 6 filas

-- RPCs con rango vacío no deben crashear
select * from public.top_wasted_search_terms('2030-01-01', '2030-01-02');
select * from public.underperforming_ad_groups('2030-01-01', '2030-01-02');
select * from public.low_quality_keywords('2030-01-01', '2030-01-02');
select * from public.geo_outliers('2030-01-01', '2030-01-02');

-- Refresh debe funcionar (puede tardar)
select public.refresh_all_materialized_views();
```

---

## PR 2 — Reportes en GAds + extender Apps Script

**No requiere Claude Code.** Es trabajo de consola GAds + un script.

### 5 reportes a crear en Google Ads

Mismo patrón que el reporte de campañas que ya está andando:

| # | Nombre en GAds | Vista | Carpeta destino |
|---|---|---|---|
| 1 | `Plasmart - Ad Groups Daily` | Ad Group Performance | `/plasmart-reports/gads/ad_groups/` |
| 2 | `Plasmart - Keywords Daily` | Keyword Performance | `/plasmart-reports/gads/keywords/` |
| 3 | `Plasmart - Search Terms Daily` | Search Terms Report | `/plasmart-reports/gads/search_terms/` |
| 4 | `Plasmart - User Location Daily` | User Location Report | `/plasmart-reports/gads/geo/` |
| 5 | `Plasmart - Asset Groups Daily` | Asset Group Performance | `/plasmart-reports/gads/asset_groups/` |

**Configuración común:**
- Rango: Últimos 2 días (overlap de 1 día por seguridad).
- Frecuencia: Diaria, 04:00 ART (antes del cron de ingesta 06:00 ART).
- Formato: Google Sheets.

**Columnas mínimas por reporte:**
- **Ad Groups:** Date, Campaign, Campaign ID, Campaign type, Ad group, Ad group ID, Ad group status, Impressions, Clicks, Cost, Conversions, Conv. value.
- **Keywords:** Date, Campaign, Campaign ID, Ad group, Ad group ID, Keyword, Keyword ID, Match type, Status, Quality score, Impressions, Clicks, Cost, Conversions, Avg. position.
- **Search Terms:** Date, Campaign, Campaign ID, Ad group, Ad group ID, Search term, Match type, Keyword (matched), Keyword ID (matched), Impressions, Clicks, Cost, Conversions.
- **User Location:** Date, Campaign, Campaign ID, Region (User location), Region ID, Country, Impressions, Clicks, Cost, Conversions.
- **Asset Groups:** Date, Campaign, Campaign ID, Asset group, Asset group ID, Status, Impressions, Clicks, Cost, Conversions.

### Crear subcarpetas en Drive

```
/plasmart-reports/gads/
  ad_groups/        (nueva)
  keywords/         (nueva)
  search_terms/     (nueva)
  geo/              (nueva)
  asset_groups/     (nueva)
  (campañas viejas siguen donde estaban)
```

Compartirlas con el mismo Service Account que ya lee la carpeta padre
(hereda permisos al estar adentro).

### Extender el Apps Script existente

El script actual mueve los Sheets de `Plasmart - Campaigns Daily` a su
carpeta. Agregarle el mapeo:

```javascript
const FOLDER_MAP = {
  'Plasmart - Campaigns Daily': 'campaigns',     // existente
  'Plasmart - Ad Groups Daily': 'ad_groups',     // nuevo
  'Plasmart - Keywords Daily': 'keywords',       // nuevo
  'Plasmart - Search Terms Daily': 'search_terms', // nuevo
  'Plasmart - User Location Daily': 'geo',       // nuevo
  'Plasmart - Asset Groups Daily': 'asset_groups' // nuevo
};

// El loop existente debería matchear por nombre del Sheet adjunto contra
// FOLDER_MAP y mover a `/plasmart-reports/gads/${folder}/`.
```

### Validación

Día siguiente a la configuración, verificar que cada subcarpeta tiene un
archivo nuevo del día anterior. El de search_terms va a tener cientos a
miles de filas, los otros mucho menos.

---

## PR 3 — Extender `ingest-reports`

### Archivos modificados

`supabase/functions/ingest-reports/index.ts`

### Archivos nuevos (opcional, si conviene factorizar)

- `supabase/functions/ingest-reports/processors/ad-groups.ts`
- `supabase/functions/ingest-reports/processors/keywords.ts`
- `supabase/functions/ingest-reports/processors/search-terms.ts`
- `supabase/functions/ingest-reports/processors/geo.ts`
- `supabase/functions/ingest-reports/processors/asset-groups.ts`
- `supabase/functions/ingest-reports/lib/ar-regions.ts`

(Si la función actual es un único archivo grande, conviene factorizar
ahora. Si ya tiene una estructura modular, sumar los nuevos al patrón
existente.)

### Cambios conceptuales

1. **Listar las 5 subcarpetas nuevas** además de las que ya procesa.
2. **Orden de procesamiento obligatorio** (FKs):
   ```
   campaigns         (existente, sin cambios)
   ad_groups         → dim_ad_group(group_type='ad_group') + fact_ad_group_daily
   asset_groups      → dim_ad_group(group_type='asset_group') + fact_ad_group_daily
   keywords          → dim_keyword + fact_keyword_daily
   search_terms      → fact_search_term_daily (busca matched_keyword_id)
   geo               → fact_campaign_geo_daily
   ```
3. **Edge case search_terms:** si llega un `Keyword ID (matched)` que no
   está en `dim_keyword` todavía, guardar `matched_keyword_id = null` y
   loggear. No reventar la transacción.
4. **Logging granular en `ingestion_log`:** una fila por archivo con
   `source` específico (`gads_ad_groups`, `gads_keywords`,
   `gads_search_terms`, `gads_geo`, `gads_asset_groups`).
5. **Refresh de MVs al final** — `refresh_all_materialized_views()` ya
   incluye las nuevas (PR 1).

### Tabla de mapeo de provincias

`supabase/functions/ingest-reports/lib/ar-regions.ts`:

```typescript
export const AR_REGIONS: Record<string, string> = {
  'Buenos Aires': 'AR-B',
  'Ciudad Autónoma de Buenos Aires': 'AR-C',
  'Catamarca': 'AR-K',
  'Chaco': 'AR-H',
  'Chubut': 'AR-U',
  'Córdoba': 'AR-X',
  'Corrientes': 'AR-W',
  'Entre Ríos': 'AR-E',
  'Formosa': 'AR-P',
  'Jujuy': 'AR-Y',
  'La Pampa': 'AR-L',
  'La Rioja': 'AR-F',
  'Mendoza': 'AR-M',
  'Misiones': 'AR-N',
  'Neuquén': 'AR-Q',
  'Río Negro': 'AR-R',
  'Salta': 'AR-A',
  'San Juan': 'AR-J',
  'San Luis': 'AR-D',
  'Santa Cruz': 'AR-Z',
  'Santa Fe': 'AR-S',
  'Santiago del Estero': 'AR-G',
  'Tierra del Fuego': 'AR-V',
  'Tucumán': 'AR-T',
};

export function normalizeRegion(regionName: string | null): {
  code: string;
  name: string | null;
} {
  if (!regionName) return { code: 'unknown', name: null };
  const trimmed = regionName.trim();
  const code = AR_REGIONS[trimmed];
  return code
    ? { code, name: trimmed }
    : { code: 'unknown', name: trimmed };
}
```

### Validación

```sql
-- Después de una corrida exitosa:
select source, count(*), max(processed_at)
from public.ingestion_log
where processed_at > now() - interval '1 day'
group by source;
-- Esperado: filas para gads_campaigns + 5 sources nuevos

select count(*) from public.fact_search_term_daily
where date = current_date - 1;
-- Esperado: cientos o miles

-- Re-correr la función debe ser idempotente (sin duplicados)
```

---

## PR 4 — Drill-down inline en `/dashboard/detalle`

**Esperar 2-3 días de ingesta en prod antes de empezar.**

### Archivos nuevos

```
components/drill-down/
  campaign-expand-row.tsx       (client)
  ad-group-subtable.tsx         (server)
  ad-group-detail-sheet.tsx     (client)
  keyword-list.tsx              (server)
  search-term-list.tsx          (server)
  geo-modal.tsx                 (client wrapper + server inside)
  empty-states.tsx              (server, mensajes vacíos)
```

### Archivos modificados

- `app/(dashboard)/dashboard/detalle/page.tsx` — sumar drill-down a la
  tabla de campañas GAds.
- `components/campaign-table.tsx` (o como se llame la tabla actual) —
  sumar columna de expand y botón "Ver geo".
- `lib/queries.ts` — sumar funciones nuevas (ver abajo).
- `lib/types.ts` — sumar tipos `AdGroupSummary`, `KeywordSummary`,
  `SearchTermRow`, `GeoSummary`.

### Funciones nuevas en `lib/queries.ts`

Seguir el patrón de las queries existentes (Supabase server client,
selecciones via `from('mv_...')` cuando aplique).

```typescript
export async function getAdGroupsByCampaign(
  campaignId: string,
  dateRange: DateRange,
): Promise<AdGroupSummary[]>;

export async function getKeywordsByAdGroup(
  adGroupId: string,
  dateRange: DateRange,
): Promise<KeywordSummary[]>;

export async function getSearchTermsByAdGroup(
  adGroupId: string,
  dateRange: DateRange,
  options: {
    sortBy?: 'cost' | 'clicks' | 'impressions';
    onlyWaste?: boolean;
    limit?: number;
  },
): Promise<SearchTermRow[]>;

export async function getGeoByCampaign(
  campaignId: string,
  dateRange: DateRange,
): Promise<GeoSummary[]>;
```

### UX del drill-down

**Por tipo de campaña:**

- **SEARCH / DEMAND_GEN:** flecha de expand → sub-tabla con ad groups.
  Click en un ad group → Sheet/Drawer lateral con dos tabs:
  - **Palabras clave:** keyword + match_type + QS + métricas.
  - **Términos de búsqueda:** default sort por costo desc, toggle "Solo
    sin conversiones" prendido por default.
- **PMAX:** expand → sub-tabla con asset groups. El ícono de expand en
  cada asset group debe estar **deshabilitado** (no hay nivel siguiente).
- **Geo:** botón aparte por fila (independiente del expand). Ícono de
  pin/mapa → modal con tabla por provincia ordenada por costo desc.

**Performance:**
- Queries del drill-down corren **lazy**, solo al expandir.
- Search terms con `limit: 100` por defecto + paginación si es necesario.

**Estados vacíos** (todos deben tener un mensaje amigable):
- Campaña sin ad groups en el período.
- Ad group sin keywords (caso PMax mal clasificado o display).
- Search terms vacíos.
- Geo vacío.

**Mobile:**
- El Sheet lateral en mobile pasa a ser bottom drawer full-screen.
- Si la UX en mobile complica mucho, esconder el botón "Ver geo" en
  viewport <640px (decidir en review).

### Estilo

- Tremor + Recharts. **No** crear charts SVG vanilla nuevos.
- Paleta industrial-azul actual (la que viene de Tremor desde PR #6).
- Copy en español rioplatense.

### Validación

- Expandir una campaña SEARCH muestra ad groups en <1s.
- Sheet con tabs funcional.
- Search terms abre con sort y filtro correctos por default.
- PMax muestra asset groups sin permitir drill-down adicional.
- Modal de geo abre y se cierra limpio.
- Estados vacíos amigables.
- No hay regresión en el resto de `/dashboard/detalle`.

---

## PR 5 — IA enriquecida en `/api/analyze`

### Archivos modificados

- `app/api/analyze/route.ts` — sumar fetch de hallazgos profundos y flag
  `has_deep_data` al hash.
- `lib/ai/prompt.ts` — sumar bloque condicional.
- `lib/ai/cache-hash.ts` (o donde esté la función de hash) — sumar
  `hasDeepData` al input.

### Archivos nuevos

- `lib/ai/deep-findings.ts` — fetch + formateo de las 4 RPCs nuevas.

### Patrón en `app/api/analyze/route.ts`

Pseudocódigo de los cambios:

```typescript
// Después de obtener KPIs y top campañas, antes de armar el prompt:
const [wasted, underperf, lowQS, geoOut] = await Promise.all([
  supabase.rpc('top_wasted_search_terms', { p_start, p_end, p_limit: 5 }),
  supabase.rpc('underperforming_ad_groups', { p_start, p_end }),
  supabase.rpc('low_quality_keywords', { p_start, p_end }),
  supabase.rpc('geo_outliers', { p_start, p_end }),
]);

const deepFindings = {
  wastedSearchTerms: wasted.data ?? [],
  underperformingAdGroups: underperf.data ?? [],
  lowQualityKeywords: lowQS.data ?? [],
  geoOutliers: geoOut.data ?? [],
};

const hasDeepData = Object.values(deepFindings).some(arr => arr.length > 0);

// Hash incorpora hasDeepData → invalida caches viejos en primera corrida
const cacheKey = hashFilters({ ...filters, maxDate, hasDeepData });

// ...resto del flujo existente, pasando deepFindings al builder del prompt
```

### Bloque nuevo en `lib/ai/prompt.ts`

Inyectar después del bloque de KPIs/top campañas, antes del bloque de
formato. Solo si `hasDeepData === true`:

```markdown
### Hallazgos profundos Google Ads

Los siguientes datos vienen del análisis a nivel ad group, keyword y geo.
Usalos para enriquecer las recomendaciones cuando sean relevantes. Citá
nombres concretos (campaña, ad group, search term, provincia) y números.

**Top 5 términos de búsqueda gastando sin convertir:**
{si la lista está vacía, omitir esta sub-sección}
{tabla markdown: search_term · ad_group · campaign · costo ARS · clicks}

**Ad groups con CTR <50% del promedio de su campaña:**
{si vacía, omitir}
{tabla: ad_group · campaign · ctr · ctr_promedio_campaña · costo}

**Palabras clave con Quality Score <5 gastando más de 500 ARS:**
{si vacía, omitir}
{tabla: keyword · match_type · ad_group · QS · costo}

**Provincias con CPA >2x el promedio nacional de la campaña:**
{si vacía, omitir}
{tabla: provincia · campaign · cpa_provincia · cpa_promedio · costo · conversiones}
```

Si las 4 listas vienen vacías individualmente, el bloque completo se omite
(equivale a `hasDeepData === false`).

### Validación

- Análisis post-deploy menciona search terms o provincias por nombre con
  números concretos.
- Cache se invalida automáticamente la primera vez (hash distinto).
- Período sin datos profundos cae al comportamiento anterior sin error.
- Costo por análisis se mantiene similar (USD 0.02–0.05). El prompt crece
  pero no se duplica.
- `/admin` muestra los análisis nuevos en el log de IA.

### Decisión adicional: Corey Haines

`/api/corey-haines` (la otra pestaña de IA) **no se toca en v1.3**. Si
después se quiere, se aplica el mismo patrón (mismo `deepFindings`,
distinta forma de inyectarlos en el prompt). Dejarlo fuera del scope.

---

## Convenciones del repo (recordatorio)

- Idioma: código en inglés, UI/copy/commits/PRs en español rioplatense.
- TypeScript strict, no `any`.
- Server Components por default, Client solo donde haya interactividad.
- Tremor + Recharts para todo lo nuevo.
- Paleta industrial-azul actual (no cambiar en v1.3).
- Migraciones versionadas con timestamp `YYYYMMDDhhmmss_descripcion.sql`.
- Commits formato `tipo(scope): descripción` en español.

---

## Prompts sugeridos para Claude Code

Uno por PR. Asumen que estás en la branch correcta y con el doc abierto.

### PR 1

> Creá la migración `supabase/migrations/20260513120000_v13_gads_deep.sql`
> siguiendo exactamente el contenido del bloque SQL del PR 1 en
> `docs/v1.3-deltas.md`. Antes de escribir las RLS policies, leé la
> migración `20260507220902_initial_schema.sql` para copiar el patrón
> exacto de policy con `@transfil.com.ar`. Al actualizar
> `refresh_all_materialized_views`, leé primero la definición actual y
> sumá las 3 refresh nuevas sin perder las existentes. Aplicá local con
> `supabase db reset` o `supabase migration up` y corré las queries de
> validación del documento.

### PR 3

> Extendé `supabase/functions/ingest-reports/index.ts` para procesar las 5
> nuevas subcarpetas de Drive (ad_groups, keywords, search_terms, geo,
> asset_groups). Mantené el patrón de upserts idempotentes que ya usa.
> Respetá el orden de FKs descrito en el plan v1.3 PR 3. Creá el módulo
> `lib/ar-regions.ts` con el mapeo del documento. Loggeá granular en
> `ingestion_log` con un `source` por subcarpeta. Llamá a
> `refresh_all_materialized_views` al final. Antes de empezar, leé la
> estructura actual del archivo para decidir si factorizar en submódulos
> o sumar al patrón existente.

### PR 4

> Implementá el drill-down inline en `/dashboard/detalle` según PR 4 de
> `docs/v1.3-deltas.md`. Creá los componentes en `components/drill-down/`,
> sumá las 4 queries a `lib/queries.ts` y los tipos a `lib/types.ts`.
> Modificá `app/(dashboard)/dashboard/detalle/page.tsx` y el componente
> de tabla para incluir el expand y el botón "Ver geo". Manejá todos los
> estados vacíos listados. Usar Tremor + Recharts; nada de SVG vanilla.
> Antes de arrancar, revisá `app/(dashboard)/dashboard/detalle/page.tsx`
> y `components/campaign-table.tsx` (o equivalente) para entender el
> patrón actual.

### PR 5

> Enriquecé `/api/analyze/route.ts` con el bloque de hallazgos profundos
> según PR 5 de `docs/v1.3-deltas.md`. Creá `lib/ai/deep-findings.ts` con
> la lógica de fetch y formateo. Modificá `lib/ai/prompt.ts` para inyectar
> el bloque condicional. Sumá `hasDeepData` al hash de cache en
> `lib/ai/cache-hash.ts` (o donde esté la función). No tocar
> `/api/corey-haines`. Antes de arrancar, leé el prompt actual y la
> función de hash para preservar todo lo existente.

---

## Backlog explícito v1.4 (no v1.3)

- Geo a nivel ciudad.
- Device y horario.
- Landing pages cruzadas con GA4.
- Assets individuales de PMax (texto, imagen, video).
- "Auditoría GAds" como pestaña IA dedicada.
- Aplicar `deepFindings` a `/api/corey-haines`.

---

*Fin del adendum v1.3.*
